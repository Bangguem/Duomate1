name: duo-mate-server
app: nodejs@18

build:
  commands:
    - npm install
  working_dir: server

run:
  command: node index.js
  port: 3000
  working_dir: server

routes:
  - path: /
    target_port: 3000
