name: duo-mate-client
app: nodejs@18

build:
  commands:
    - npm install
    - npm run build
  working_dir: client

run:
  command: npx serve -s dist
  port: 3000
  working_dir: client

routes:
  - path: /
    target_port: 3000
